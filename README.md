# Flask Server for Datnek Test

## How to use it locally
- Clone the repository
- Go to root directory
- Create a virtual python environment
- Run pip install -r requirements.txt
- Run the app using: python -m flask run